#!/usr/bin/env python3
# coding=utf-8

# python identifiers: 
#   variable/function/class/module/object name/identify 
#       first + followed: first(litter/ _) + followed(litter/_/number)
#   name standard:
#       ClassName(uppercase) other with lowercase letter
#   private element:
#       first(_)
#   strong private element:
#       first(__)

# reserved world:

import sys

try:
    # open file stream
    file = open(file_name, "w")

except IOERROR:
    print("There was an error writing to", file_name)
    sys.exit()

print("Enter '", file_finish,)
print  "' When finished"
